export default class User {
  constructor(user_name, full_name, email, password) {
    this.user_name = user_name;
    this.full_name = full_name;
    this.email = email;
    this.password = password;
  }
}
